exports.connectToDatabase = async (banco) => {
  console.log("Se conectando com o banco de dados  " + banco);
};

exports.desconectToDatabase = async (banco) => {
  console.log("Se desconectando com o banco de dados " + banco);
};
